{
  "routes": [
    {
      "method": "GET",
      "path": "/dentists",
      "handler": "dentist.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/dentists/count",
      "handler": "dentist.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/dentists/:id",
      "handler": "dentist.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/dentists",
      "handler": "dentist.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/dentists/:id",
      "handler": "dentist.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/dentists/:id",
      "handler": "dentist.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
